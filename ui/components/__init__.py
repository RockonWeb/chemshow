"""
UI компоненты для Справочника соединений

This package contains all the UI components for the compound reference application.
Import the specific modules directly rather than through this __init__.py file
to avoid circular import issues.
"""

# Explicitly avoid importing anything here to prevent circular import issues
# Import modules directly: from components.module_name import function_name
